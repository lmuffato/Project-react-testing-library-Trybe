import React from 'react';
// import { MemoryRouter } from 'react-router-dom';
import { screen, render } from '@testing-library/react';
import NotFound from '../components/NotFound';
import renderWithRouter from './renderWithRouter';

test('Testa se aparece card com as informações do pokémon', () => {});

test('Testa se contém um link de navegação para exibir detalhes', () => {});

test('Testa se o clique redireciona para a página de detalhes', () => {});

test('Testa se a URL exibida muda para outro pokémon', () => {});

test('Testa se aparece ícone de estrela nos favoritos', () => {});